{{- if and .Values.captain.enabled (and .Values.captain.secret.enabled .Values.captain.secret.minio.enabled) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.captain.secret.minio.secretName }}
  labels:
    {{- include "AGH2.labels" . | nindent 4 }}
stringData:
  {{- with .Values.captain.secret.minio }}
  capt-minio-user: {{ (required "captain.secret.minio.user is required" .user) | quote }}
  capt-minio-password: {{ (required "captain.secret.minio.password is required" .password) | quote }}
  {{- end }}
{{- end }}
