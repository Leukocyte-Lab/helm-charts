{{- if .Values.template.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: template
  labels:
    {{- include "AGH2.labels" . | nindent 4 }}
spec:
  selector:
    app: template
  ports:
    - port: 50005
      targetPort: grpc
{{- end }}