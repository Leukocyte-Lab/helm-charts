{{- if .Values.blender.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: blender
  labels:
    {{- include "AGH2.labels" . | nindent 4 }}
spec:
  selector:
    app: blender
  ports:
    - port: 50005
      targetPort: grpc
{{- end }}